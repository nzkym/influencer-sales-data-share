# Chrome 디버그 포트(CDP) 할당표

> **새 프로젝트에서 `--remote-debugging-port`로 Chrome을 띄우는 자동화를 만들기 전에 반드시 이 표부터 확인할 것.** 이미 쓰이는 포트를 몰래 재사용하면, 새 Chrome이 안 뜨고 기존 프로그램의 브라우저에 조용히 얹혀서 엉뚱한 탭을 조작하는 사고가 남(2026-07-29, 쇼피 프로젝트가 "업체전반정보수집"의 플로우 크롬에 잘못 접속했던 사고로 발견).

## 현재 할당 (2026-07-29 기준)

| 포트 | 프로그램 | 프로필 경로 | 실행 파일 |
|---|---|---|---|
| 9222 | 업체전반정보수집 및 개선프로그램 — 카카오용 | `C:\클로드프로그램_C드라이브\업체전반정보수집 및 개선프로그램\chrome_profile_kakao` ⚠️2026-08-02 C드라이브로 이동 | `업체전반정보수집 및 개선프로그램\start_persistent_browsers.py` |
| 9223 | 업체전반정보수집 및 개선프로그램 — 플로우(Flow)용 | `C:\클로드프로그램_C드라이브\업체전반정보수집 및 개선프로그램\chat_data\flow\_browser_profile_chrome` ⚠️2026-08-02 C드라이브로 이동 | `업체전반정보수집 및 개선프로그램\start_persistent_browsers.py` |
| 9224 | ai MD 통합 (지마켓·옥션 / 11번가 / 카카오광고 / 구글애드워즈 / 네이버광고 — 광고채널 5종 공용) | `C:\Users\Admin\AppData\Local\AIChannelsProfile` | `ai MD\AI채널학습용_크롬.ps1`(⚠️.bat 아님, cmd.exe 긴 유니코드 파싱 문제로 폐기됨) → `크롬_탭_자동정리\크롬_안전실행.py` 호출, 5채널 URL 전부 탭으로 오픈 |
| **9225** | 큐텐재팬 QSM (2026-07-29부로 9222→9225 변경) | `C:\Users\Admin\AppData\Local\QSMAutomationProfile` | `큐텐재팬_채널운영\큐텐QSM_자동화크롬.ps1`(⚠️.bat 아님) → `크롬_탭_자동정리\크롬_안전실행.py` 호출, `--app=` 모드(작업표시줄 아이콘 구분) |
| **9226** | 쇼피 싱가포르 채널관리 (2026-07-29부로 9223→9226 변경) | `C:\클로드프로그램_C드라이브\쇼피_싱가포르_채널관리\chrome_debug_profile` ⚠️2026-08-02 C드라이브로 이동 | `쇼피_싱가포르_채널관리\디버그크롬_열기.ps1`(⚠️.bat 아님) → `크롬_탭_자동정리\크롬_안전실행.py` 호출, `--app=` 모드(작업표시줄 아이콘 구분) |
| 9333 | 네이버 리뷰답변 자동화(naver_review_reply) | `naver_review_reply\data\chrome_profile` | `naver_review_reply\naver_login.py` (`_DEBUG_PORT`) |
| **9227** | 자사몰 SEO 최적화(seo최적화) — 네이버 서치어드바이저/구글 서치콘솔/다음·Bing 웹마스터 로그인용 | `C:\클로드프로그램_C드라이브\seo최적화\chrome_profile` | `기타\seo최적화\scripts\seo_로그인_브라우저_열기.py` |
| **9228** | 메타(페이스북) 광고 채널 학습 + 픽셀 점검 — 비즈니스 관리자/광고 관리자/이벤트 관리자 | `C:\클로드프로그램_C드라이브\메타_MD\chrome_profile` | `ai MD\메타 MD\메타_디버그크롬_열기.ps1` → `크롬_탭_자동정리\크롬_안전실행.py` 호출 |
| **9229** | 11번가 야간입찰 사다리실험(`night_bid_ladder.py`) 전용 — 2026-08-14, 공용9224에서 이틀연속 연결사고 나서 분리 | `C:\클로드프로그램_C드라이브\11번가_야간실험_전용크롬\chrome_profile` | `11번가_야간실험_전용크롬\11번가야간실험_크롬_열기.ps1` |
| **9230** | 아마존 셀러센트럴(아마존MD) — 2026-08-14 신규, 2단계인증(OTP)으로 자동로그인 불가 예상, 최초 로그인은 사장님이 직접+"기기 신뢰" 체크 필요 | `C:\클로드프로그램_C드라이브\아마존_MD\chrome_profile` | `ai MD\아마존MD\아마존_디버그크롬_열기.ps1` → `크롬_탭_자동정리\크롬_안전실행.py` 호출, `--app=` 모드 |
| **9231** | 건강기능식품협회(KHFF) 회원사 사이트 — 2026-08-17 신규, 아이디/비번 로그인(ID/PW form login) | `C:\클로드프로그램_C드라이브\건강기능식품협회_사이트\chrome_profile` | `기타\건강기능식품협회 사이트\khff_login.py` |
| **9232** | 쇼피 싱가포르 — **Claude 애드혹 조사 전용** (9226은 4개 예약자동화 공용이라 충돌 잦음, 2026-08-19 사장님 지시) | `C:\클로드프로그램_C드라이브\쇼피_싱가포르_채널관리\chrome_debug_profile_manual` (9226 프로필에서 로그인쿠키만 선별복사, 캐시 제외) | `쇼피_싱가포르_채널관리\스크립트_아카이브\shopee_sg_manual_browser.py` — **필요할 때만 켜기**, `ensure_browser()`/`close_browser()` (아마존9230 패턴) |
| **9233** | 유통기한_관리 — 메이크샵(mindpharm) 소비기한 주간 자동갱신 전용 (2026-08-22 신규, 기존엔 9227을 같이 썼는데 그 시간대에 다른 작업이 9227을 쓰면 탭이 꼬이는 충돌이 있어 분리) | `C:\클로드프로그램_C드라이브\유통기한_관리\chrome_profile` (⚠️최초 1회 9227 프로필에서 로그인쿠키/Login Data 선별복사 또는 수동 로그인 필요 - 아직 부트스트랩 전) | `유통기한_관리\scripts\makeshop_browser.py` — **필요할 때만 켜기**, `ensure_browser()` (아마존9230 패턴), `update_expiry.py`의 `get_driver()`가 자동 호출 |

### ⚠️ C드라이브 이동 안내 (2026-08-02, 다른 컴퓨터에서 볼 때 필독)

9222/9223/9226의 크롬 프로필 폴더는 원래 이 "클로드 프로그램"(Dropbox) 폴더 안에 있었으나, 파일 개수가 너무 많아(캐시 파일 수천~수만개, 총 9.3GB) Dropbox 동기화 부하(RAM 사용량 급증)의 원인이 되어 **이 컴퓨터(MIRAE-201010N, 서버용)의 `C:\클로드프로그램_C드라이브\` 밑으로 이동**했다. Dropbox가 동기화하지 않는 위치라:
- **다른 컴퓨터에는 이 폴더들이 없다** — 로그인 세션 캐시일 뿐이라 원래도 다른 컴퓨터에서 쓸 이유가 없는 데이터임(자동화가 이 서버 컴퓨터에서만 도니까).
- 다른 컴퓨터에서 새로 같은 자동화를 켜야 한다면, 그 컴퓨터의 로컬 경로(예: 그 컴퓨터의 C드라이브)에 새 프로필로 새로 로그인해서 쓰면 됨 — Dropbox로 옮겨올 필요 없음.
- 스크립트/코드 자체(.py, .bat, .md)는 전부 그대로 Dropbox로 동기화되므로 다른 컴퓨터에서 코드 수정/조정하는 데는 전혀 지장 없음.

| **9234** | 쿠팡 서플라이허브 — 식약처 이력추적 납품 출고 수집 전용 (2026-09-10 신규, 기존 headless Playwright → 상주 Chrome으로 전환해 Akamai WAF 차단 방지) | `C:\클로드프로그램_C드라이브\쿠팡서플라이허브\chrome_profile` | `식약처_이력추적\쿠팡서플라이허브_크롬_열기.ps1` → `크롬_안전실행.py`, `--app=` 모드. 자동화 실행 시 `coupang_browser.ensure_browser()`가 필요할 때만 켬. **최초 1회 수동 로그인 필요.** |

## 다음 빈 포트

새 프로젝트는 **9235**부터 순서대로 쓸 것.

## 자동 관리 (2026-07-27 추가)

- **탭 자동정리**: `크롬_탭_자동정리\cleanup_all_debug_tabs.py`가 이 표를 그대로 읽어서 3시간마다(작업 스케줄러 `크롬탭_자동정리_3시간마다`) 모든 등록 포트를 돌며 같은 사이트 중복 탭/에러 탭만 정리한다. 브라우저 자체는 안 끔. **새 포트를 이 표에 추가하면 별도 설정 없이 자동으로 정리 대상에 포함됨.**
- **로그온 시 브라우저 자동재시작**: 재부팅/재로그인 후 1분 뒤 아래 작업들이 각 프로젝트의 디버그 크롬을 자동으로 다시 켬(고정 프로필이라 로그인 세션도 그대로 복원됨):
  - `카카오플로우브라우저시작` (9222, 9223 — 기존에 있던 것)
  - `AI채널학습용_크롬_시작` (9224)
  - `큐텐QSM_브라우저_시작` (9225)
  - `쇼피SG_브라우저_시작` (9226)
  - `메타MD_브라우저_시작` (9228)
  - `KHFF협회사이트_크롬_시작` (9231)
  - 9333(naver_review_reply)은 매일 11:50 `NaverReviewDaily` 작업이 자체적으로 브라우저를 다루므로 별도 등록 안 함
  - ⚠️ **9230(아마존)은 일부러 로그온 시작작업을 만들지 않았다** — 아래 참고
  - 새 포트를 추가하는 프로젝트는 이 패턴대로 로그온 트리거 작업도 같이 등록할 것
    (단, 상시 띄워둘 필요가 있는 채널인지 먼저 판단할 것)

## ⭐ 상시 실행 vs 필요할 때만 실행 (2026-08-18 방침)

크롬창이 많이 떠 있어 **메모리/CPU 부담이 커졌다**는 사장님 지적에 따라,
"모든 채널을 24시간 띄워둔다"는 기존 전제를 재검토함.

**원칙**: 정기 스케줄 자동화가 실제로 도는 채널만 상시 유지하고, 사람이나
AI가 가끔 들여다보는 채널은 **필요할 때만 켠다(on-demand).**

| 방식 | 대상 | 이유 |
|---|---|---|
| 상시 유지 + 워치독 | 9222(카카오), 9223(플로우) | 수시로 도는 수집 자동화가 붙어있음 |
| **필요할 때만 켜기** | **9230(아마존MD)** | 정기 자동화 아직 없음, 조사·학습 목적으로만 사용 |
| **필요할 때만 켜기** | **9232(쇼피SG-Claude조사용)** | 9226(예약자동화 4개 공용)과의 충돌 회피 전용, 2026-08-19 신규 |

**2026-08-19: 9232 신규 — 쇼피SG 포트 충돌 해결.** 9226을 KOL파이프라인/CS챗워처/주간체크/애드혹 스크립트 4개가 공유하다 보니 Claude가 즉석 조사(브랜드등록 확인 등)를 하려고 붙을 때마다 CDP 연결 충돌이 잦았음(사장님 지적: "다른 크롬포트를 쓰던가해서 이문제 해결"). 원인은 외부 프로그램이 아니라 **9226을 쓰는 쇼피SG 자체 자동화 4개끼리의 경합**이었음(CS챗워처가 1분마다 붙는 게 가장 잦은 충돌원 — `project_shopee_cs_chat_automation` 메모리의 "1864회 연결실패" 기록 참고).

9232 프로필은 9226 프로필에서 **로그인쿠키(Cookies, Local State)와 Login Data/Web Data/Preferences만 선별복사**해서 만듦(캐시류 폴더 4.1GB 전부 제외, 순복사 후 총량 대폭 축소). Cookies 파일은 살아있는 9226 크롬이 SQLite 배타 잠금을 걸고 있어 바로 복사가 안 됐고, 9226을 정상 종료(WM_CLOSE, 쿠키 플러시)→복사→즉시 재기동하는 방식으로 해결(재기동 후 로그인 유지 실측 확인, 2026-08-19). 이 패턴은 향후 다른 포트의 프로필을 복제할 때도 그대로 쓸 수 있음.

9226(예약자동화 4개 전용)과 9232(Claude 애드혹 조사 전용)로 완전히 분리했으니, **앞으로 Claude가 직접 즉석 조사할 땐 9232를 쓸 것** — 9226은 절대 건드리지 않는다.

**⭐ 아마존은 사람이 켤 필요 없다 — 필요한 쪽이 자동으로 켠다.**

`ai MD\아마존MD\scripts\amazon_browser.py`가 그 역할을 한다. 아마존이 필요한
스크립트는 맨 앞에서 `ensure_browser()`만 부르면 됨:

```python
import sys
sys.path.insert(0, r"D:\...\ai MD\아마존MD\scripts")
from amazon_browser import ensure_browser, close_browser

ensure_browser()      # 꺼져 있으면 자동 기동, 켜져 있으면 그냥 통과(멱등)
# ... playwright로 9230에 connect_over_cdp 해서 작업 ...
close_browser()       # (선택) 작업 끝나면 메모리 반납
```

CLI로도 됨: `python amazon_browser.py open` / `python amazon_browser.py close`

**2026-08-18 전체 사이클 실측 검증 완료**: 꺼진 상태에서 자동 기동 → 이미
켜져 있을 때 멱등 통과 → 로그인 유지(Jeongdam Health 확인) → 정상 종료.

사람이 직접 열고 싶을 때만 쓰는 보조 수단(선택):
`ai MD\아마존MD\아마존_열기.bat` 더블클릭, 또는 `아마존_디버그크롬_열기.ps1`

**끄는 방법**: 그냥 크롬창을 닫거나 `close_browser()`. 로그인은 유지됨.

> 이 패턴(스크립트가 자기 브라우저를 스스로 기동)을 **다른 채널에도 적용**하면
> 상시 유지가 필요 없어진다. `collect_flow_chats.py`의 `restart_flow_chrome()`도
> 같은 발상.

## ⭐ 스케줄 자동화 전체에 자동 기동 적용 (2026-08-18)

크롬을 꺼둬도 스케줄 자동화가 실패하지 않도록, **13개 스케줄 작업이 브라우저를
먼저 켜고 스크립트를 돌리게** 바꿨다. 각 자동화 스크립트의 로직은 손대지 않음.

**공용 모듈** (`ai MD\_common\`):
- `ensure_browser.py` — 포트별 프로필/URL을 알고 있고, 꺼져 있으면 켠다(멱등).
  9224는 기동 후 `ai_md_자동로그인.py`까지 자동 실행.
  등록된 포트: 9224(AI MD 통합), 9225(QSM), 9226(쇼피SG), 9230(아마존)
- `run_with_browser.py` — `python run_with_browser.py <포트> <원본스크립트> [인자]`
  형태로 감싸주는 범용 러너

**적용된 스케줄 작업 13개**:
`GmarketAuctionAds_WeeklyCheck`, `GmarketAuction_NightBidBoost`,
`GmarketAuction_NightBidRevert`, `GmarketAuction_NightBidRevert_Watchdog`,
`GoogleAds_ConversionTagMonitor`, `GoogleAds_WeeklyROASCheck`,
`KakaoAds_DayBidRevert`, `KakaoAds_NightBidBoost`, `KakaoAds_WeeklyCheck`,
`NaverAds_WeeklyCheck`, `St11Ads_WeeklyCheck` (이상 9224),
`QSM_WeeklyCheck` (9225), `ShopeeSG_WeeklyCheck` (9226)

추가로 `night_bid_schedule.py`(카카오)는 스크립트 자체에도 `ensure_browser()`를
넣었다 — 기존엔 "크롬 꺼짐 → 경고만 보내고 포기"였는데, 이제 켜보고 그래도
안 되면 경고한다.

**실측 검증 (2026-08-18)**:
1. 러너 단독 실행 OK
2. 스케줄러를 통한 실행 OK (`LastTaskResult=0`)
3. **9224를 실제로 끈 상태에서 스케줄 작업 실행 → 자동 기동되어 정상 완료**
4. 재기동 후 **5개 채널(네이버/카카오/구글/11번가/ESM) 로그인 전부 유지 확인**
   (9224 프로필의 각 채널 쿠키가 만료 ~400일짜리 영구쿠키인 것도 확인)

⚠️ **작업 인자를 일괄 수정할 때 주의**: 이때 쓴 임시 .ps1에 BOM이 없어서
러너 경로가 빈 문자열로 들어가 13개 작업이 한 번 깨졌다(즉시 복구). 수정 후엔
반드시 `Get-ScheduledTask ... | Format-List Arguments`로 되읽어 확인할 것.

**아마존 세션 지속성 (2026-08-18 실측 검증 완료)**: 프로필의 아마존 로그인
쿠키(`session-token`, `x-main`, `at-main`, `sess-at-main`, `ubid-main` 등)가
전부 **만료 2027-08의 영구 쿠키**다. 실제로 크롬을 정상 종료한 뒤 다시 켜서
**Jeongdam Health 계정 로그인이 그대로 유지되는 것을 화면으로 확인**했다.
즉 2단계인증(OTP) 채널이지만 **껐다 켜도 재로그인이 필요 없다** —
그래서 상시 띄워둘 이유가 없다. (쿠키 만료가 임박하는 2027년 중반쯤엔
재로그인이 필요할 수 있음)

## 🛑 5분 워치독 — 2026-08-18부로 **비활성화됨**

`CDP_브라우저_워치독_5분` (`업체전반정보수집 및 개선프로그램\watchdog_browsers.py`)
는 5분마다 죽은 브라우저를 되살리는 작업이었으나, **메모리/CPU 부담을 줄이려는
방침과 정면으로 충돌**해서 사장님 지시로 껐다(`Settings.Enabled = False`).

같은 이유로 `카카오플로우브라우저시작`(로그온 시 9222/9223 자동 기동)도
**비활성화**했다.

**되살리려면**: `Enable-ScheduledTask -TaskName 'CDP_브라우저_워치독_5분'`

### 워치독을 꺼도 안전한 근거 (2026-08-18 확인)

| 포트 | 쓰는 곳 | 꺼도 되는 이유 |
|---|---|---|
| 9222 (카카오) | `scrape_kakao_chat.py` | 해당 수집이 **보류 상태**(run_daily_collect.py에서 주석처리, 2단계인증 미해결) — 실질적으로 안 쓰임 |
| 9223 (플로우) | `collect_flow_chats.py` (매일 13:00) | 스크립트가 **자체적으로 `restart_flow_chrome()`을 갖고 있어** "Chrome 꺼짐 → 자동 시작"을 스스로 처리함 |
| 9230 (아마존) | 조사·학습 용도 | 정기 자동화 없음, 필요할 때만 켜면 됨 |

⚠️ **미검증 항목**: 9223은 스크립트에 자동 기동 코드가 있는 걸 **코드로만
확인**했고 실제로 브라우저가 꺼진 상태에서 13:00 수집이 성공하는지는 아직
안 봤다. **2026-08-19 13:00 정기수집이 첫 실전 검증 시점** — 실패하면
텔레그램 알림이 오므로 그때 확인할 것. 문제가 있으면 `카카오플로우브라우저시작`을
다시 켜거나 `run_daily_collect.py` 앞부분에 브라우저 기동을 추가하면 된다.

### 워치독 관련 2026-08-18에 고친 것 (지금은 꺼져있지만 기록용)

1. **스케줄러가 .bat을 호출해서 조용히 실패하고 있었음** — `LastTaskResult=1`,
   로그 파일이 한 번도 생성된 적 없었음(= 7/31 등록 이후 **한 번도 실제로
   동작한 적이 없었다는 뜻**). `cmd.exe /c ...bat` → `python.exe` 직접 호출로
   변경 후 `LastTaskResult=0` + 로그 정상 기록 확인.
   (`feedback_scheduled_task_bat_wrapper_korean_path` 메모리의 그 패턴)
2. **로그온 트리거 추가** — 기존엔 1회성 TimeTrigger+무한반복만 있어서
   재부팅 후 안 살아날 위험이 있었음
   (`feedback_scheduled_task_one_time_trigger_trap` 메모리 참고).

→ 나중에 워치독을 다시 켤 일이 있으면 위 두 개는 이미 고쳐져 있으므로
그대로 `Enable-ScheduledTask`만 하면 정상 동작한다.

## ⚠️ .ps1 런처는 반드시 UTF-8 **BOM**으로 저장할 것 (2026-08-18 실측)

한글 경로를 쓰는 `.ps1` 런처를 **BOM 없는 UTF-8**로 저장하면, Windows
PowerShell 5.1이 파일을 ANSI(코드페이지 949)로 잘못 읽어서 스크립트 안의
한글 경로 문자열이 파싱 시점에 이미 깨진다. 그 상태로 python.exe에 넘어가면:

```
python.exe: can't open file 'd:\\?쒕∼諛뺤뒪\\...\\?щ＼_?덉쟾?ㅽ뻾.py': [Errno 22] Invalid argument
```

- 인자 전달 방식(`& $exe $args` / 환경변수 우회 등)을 바꿔도 **해결 안 됨** —
  파일을 읽는 단계에서 이미 깨지므로 BOM을 넣는 것 외엔 방법이 없음
- 2026-08-18 기준 `아마존_디버그크롬_열기.ps1`만 BOM이 없어서 계속 실패하고
  있었고(메타/QSM/AI채널/쇼피 ps1은 전부 BOM 있음), BOM 부착 후 정상 동작 확인
- **새 .ps1 런처를 만들 때 주의**: Claude의 Write 도구는 BOM을 붙이지 않으므로,
  작성 후 반드시 아래로 BOM을 부착하고 실제로 한 번 실행해볼 것
  ```bash
  python -c "import io; p=r'<경로>'; c=io.open(p,encoding='utf-8').read().lstrip('\ufeff'); io.open(p,'w',encoding='utf-8-sig').write(c)"
  ```
- 워치독은 이 함정을 피하려고 ps1을 거치지 않고 `subprocess.Popen([python, script, ...])`
  **리스트 형태**로 직접 호출한다(파이썬은 CreateProcessW/UTF-16이라 안전).

## 포트뱃지 확장프로그램 (2026-08-06 추가)

크롬 창 화면 자체에서 몇 번 포트인지 바로 보여주는 방법(사장님 요청: "그냥 크롬창 화면자체내에서 바로 확인할수있게 하는 방법은 없어?"). `크롬_탭_자동정리\포트뱃지_생성.py`가 이 표를 읽어서 포트별로 `크롬_탭_자동정리\포트뱃지_확장프로그램\포트_<port>\`에 툴바 뱃지 확장프로그램(MV3)을 생성한다. `크롬_안전실행.py`가 실행 시 `--load-extension=`으로 이 폴더를 붙여서 띄운다.

**중요한 제약/삽질 기록:**
- **`--app=` 모드 창(QSM 9225, 쇼피SG 9226, 카카오/플로우 9222/9223)은 툴바 자체가 없어서 뱃지가 절대 안 보인다** — 대신 이미 파비콘 기반 작업표시줄 아이콘으로 구분되니 실질적 손해는 없음. 뱃지는 **일반 탭 모드 창에서만** 의미가 있다 (AI MD 9224, 메타MD 9228, naver_review_reply 9333에서 확인/적용함).
- **Chrome 최신버전(150.x)은 커맨드라인 `--load-extension`만으로는 확장프로그램이 로드되지 않는다** — 반드시 해당 프로필에서 최초 1회 `chrome://extensions` → "개발자 모드" 켜기 → "압축해제된 확장 프로그램 로드"로 수동 로드해야 그 뒤로 프로필에 영구 설치되어 재부팅 후에도 계속 로드됨(더 이상 `--load-extension` 플래그가 없어도 됨). 새 프로필로 초기화되면 이 1회 작업을 다시 해야 함.
- **뱃지가 툴바에 고정(pin)되어 보이려면** `<프로필>\Default\Preferences`의 `extensions.pinned_extensions` 배열에 확장프로그램 ID를 넣어야 함(1회, `크롬_안전실행.py`의 `patch_preferences()`가 매번 자동으로 하진 않음 - 필요하면 각 프로필별로 수동 patch).
- **MV3 서비스워커는 상시 실행이 아니라 이벤트 기반**이라 `chrome.runtime.onInstalled`만으로는 브라우저 재시작마다 뱃지가 사라짐 → `background.js`에 `onStartup` 리스너 + `setTimeout` 지연 재호출(1초/3초)까지 넣어야 재부팅 직후에도 안정적으로 뱃지가 그려짐(툴바 리페인트 타이밍 문제로 추정, 실측 확인).
- 확장프로그램 ID는 언팩 확장프로그램 경로 기준으로 고정되므로 `<프로필>\Default\Secure Preferences`의 `extensions.settings`에서 `path`로 검색하면 확인 가능(chrome://extensions 페이지 DOM에서 직접 긁는 것보다 이 방법이 안정적).

## 탭 포커스 뺏기 방지 (2026-08-13 추가)

**증상**: 포트9224(AI MD 5채널 공용)에서, 예약된 채널 스크립트가 새 탭을 열면 사장님이 다른 목적으로
보고 있던 탭(예: 메이크샵/mindpharm 관리자 로그인 중)이 화면에서 사라지고 그 채널 사이트로 화면이
확 바뀌는 문제가 실제로 있었음(사장님 보고: "지마켓옥션 프로그램이 자꾸 본인 사이트로 로그인하려고
한다"). 실측 확인 결과 `context.new_page()`로 새 탭을 만드는 순간 그 탭이 화면에 활성화되는 게
원인이었음(스크린샷으로 재현 확인, 2026-08-13).

**해결**: `ai MD\_common\tab_cleanup.py`에 `open_background_tab(context, log)` 헬퍼 추가 — 새 탭
생성 직후 즉시 원래(직전) 활성 탭으로 포커스를 복원한다. **백그라운드 상태인 새 탭에서도
goto/click/evaluate 등 조작은 문제없이 동작함을 실측으로 확인**했음(화면에 안 보여도 자동화는 정상
진행). 구글애즈/카카오광고/네이버광고/11번가/지마켓옥션 5개 스크립트 전부 이 패턴으로 수정+실측
검증 완료(각 스크립트 실행 중 다른 탭이 계속 화면에 보이는지 스크린샷으로 확인).

**새 채널/스크립트를 만들 때**: 포트9224처럼 여러 프로그램이 크롬 창 하나를 공유하는 곳에서
`context.new_page()`를 직접 쓰지 말고 반드시 `open_background_tab()`을 사용할 것 — 그래야 사장님이
그 순간 다른 탭에서 뭘 하고 있어도 화면이 안 뺏김.

## 규칙

1. 새 자동화 프로젝트를 만들 때, 이 표에 없는 포트를 골라서 쓰고 **바로 이 표에 한 줄 추가**할 것.
2. 포트+프로필 디렉토리는 항상 세트로 고정 — 같은 프로젝트는 항상 같은 포트를 씀(재로그인 부담 줄이기 위해, `feedback_selenium_persistent_profile` 메모리 참고).
3. 이미 켜져 있는 디버그 크롬 창은 절대 임의로 끄지 않는다(`feedback_never_close_persistent_browser` 메모리 참고) — 포트가 겹친 걸 발견해도 실행 중인 프로세스를 죽이지 말고, 파일(.bat/.py)의 포트 번호만 고쳐서 다음 실행부터 반영되게 한다.
4. 포트를 변경한 프로젝트는 **다음 번 그 프로젝트의 디버그 크롬을 새로 켤 때** 새 포트로 뜨게 되므로, 스케줄러로 도는 스크립트(weekly_check.py 등)가 있다면 사용자가 편한 시점에 한 번 크롬을 재시작해줘야 함 — 강제로 재시작시키지 말고 안내만 할 것.
